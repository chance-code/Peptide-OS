# Pre-commit Checks

Run all checks before committing:

1. **TypeScript**: `npx tsc --noEmit`
2. **ESLint**: `npm run lint`
3. **Build**: `npm run build`

Report any errors or warnings that need to be fixed.

If all checks pass, summarize what's ready to commit.
